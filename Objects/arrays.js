"use strict";
let fruits = ["apple" , "banana" , "orange"];
// 0, 1, 2 this is how the computer reads it 
fruits.push("grape");
//push is adding to the end of the array
//pop is removing the last item at the end of the array, no specifciation needed 
console.log(fruits);
// arrays are always in square brackets, computer reads array in square brackets
fruits.pop();
console.log(fruits);